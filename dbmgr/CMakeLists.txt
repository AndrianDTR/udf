#-------------------------------------------------------------------
# This file was automatically generated by ProjectGenerator
# which is tooling part the build system designed for GUCEF
#     (Galaxy Unlimited Framework)
# For the latest info, see http://www.VanvelzenSoftware.com/
#
# The contents of this file are placed in the public domain. Feel
# free to make use of it in any way you like.
#-------------------------------------------------------------------
#

# Configuration for module: gucefMT

set( HEADER_FILES 
	src/dberrors.h
	src/dbutils.h
	src/dbconnection.h
	src/dbtable.h
	src/tagecategory.h
	src/tcategories.h
	src/tchampionship.h
	src/tchampionshipcategories.h
	src/tchampionship.h
	src/tchampionshipjudgesmark.h
	src/tchampionshipjudgesteam.h
	src/tchampionshipteamcategories.h
	src/tchampionshipteamdancers.h
	src/tchampionshipteam.h
	src/tchampionshiptype.h
	src/tcities.h
	src/tclubs.h
	src/tcountries.h
	src/tdancers.h
	src/tdancetypes.h
	src/tgender.h
	src/tjudgescategorieshave.h
	src/tjudgescategoriesname.h
	src/tjudges.h
	src/tliga.h
	src/ttreiners.h
)

set( SOURCE_FILES 
	src/dberrors.cpp
	src/dbutils.cpp
	src/dbconnection.cpp
	src/tagecategory.cpp
	src/tcategories.cpp
	src/tchampionship.cpp
	src/tchampionshipcategories.cpp
	src/tchampionship.cpp
	src/tchampionshipjudgesmark.cpp
	src/tchampionshipjudgesteam.cpp
	src/tchampionshipteamcategories.cpp
	src/tchampionshipteamdancers.cpp
	src/tchampionshipteam.cpp
	src/tchampionshiptype.cpp
	src/tcities.cpp
	src/tclubs.cpp
	src/tcountries.cpp
	src/tdancers.cpp
	src/tdancetypes.cpp
	src/tgender.cpp
	src/tjudgescategorieshave.cpp
	src/tjudgescategoriesname.cpp
	src/tjudges.cpp
	src/tliga.cpp
	src/ttreiners.cpp
)

# Make sure the PLATFORM_HEADER_FILES variable is always defined
 set( PLATFORM_HEADER_FILES "" )

# Make sure the PLATFORM_SOURCE_FILES variable is always defined
 set( PLATFORM_SOURCE_FILES "" )

# For ease of use make a variable that has all files for this module
set( ALL_FILES ${HEADER_FILES} ${SOURCE_FILES} ${PLATFORM_HEADER_FILES} ${PLATFORM_SOURCE_FILES} )

set( MODULE_NAME "dbmgr" )

add_library( ${MODULE_NAME} SHARED ${ALL_FILES} )

include_directories( ${UDF_SOURCE_DIR}/common/src src )

add_definitions(-DDBMGR_EXPORTS)

set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -g -Wall ")

if(UDF_DEBUG STREQUAL yes)
	MESSAGE(STATUS "DEBUG BUILD.")
	ADD_DEFINITIONS(-D_DEBUG=1)
endif()

INCLUDE_DIRECTORIES(
	${MYSQL_INCLUDE_DIR}
	${BOOST_INCLUDE_DIR}
)

IF (UNIX)
	
	LINK_DIRECTORIES(${MYSQL_INCLUDE_DIR}/diver)
	
	INCLUDE_DIRECTORIES(/usr/include/mysql)
		
	ADD_DEFINITIONS(-DBIG_JOINS=1 -fno-strict-aliasing -g)
	
	TARGET_LINK_LIBRARIES(${MODULE_NAME} ${MYSQL_INCLUDE_DIR}/driver/libmysqlcppconn-static.a; mysqlclient_r)
	
	ADD_DEPENDENCIES(${MODULE_NAME} mysql_connector)

ELSE( WIN32 )
	target_link_libraries( ${MODULE_NAME} Winmm )
	MESSAGE(FATAL_ERROR "Extract MySQL connector for win32 not implemented yet.")
ELSE( WIN64 )
	target_link_libraries( ${MODULE_NAME} Winmm )
	MESSAGE(FATAL_ERROR "Extract MySQL connector for win32 not implemented yet.")
ENDIF()

