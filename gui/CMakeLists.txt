#-------------------------------------------------------------------
# This file was automatically generated by ProjectGenerator
# which is tooling part the build system designed for GUCEF
#     (Galaxy Unlimited Framework)
# For the latest info, see http://www.VanvelzenSoftware.com/
#
# The contents of this file are placed in the public domain. Feel
# free to make use of it in any way you like.
#-------------------------------------------------------------------
#

# Configuration for module: gucefGUI

set( HEADER_FILES 
	src/cdbmanager.h
	src/main.h
	src/udf.h
	src/udfAccountInfo.h
	src/udfCategoriesMngrDlg.h
	src/udfChampionshipCategoriesMngrDlg.h
	src/udfChampionshipJudgesTeamMngrDlg.h
	src/udfClubsMngrDlg.h
	src/udfDancersMngrDlg.h
	src/udfDancersTeamMngr.h
	src/udfDirectorInfo.h
	src/udfJudgesMngr.h
	src/udfMainFrameBase.h
	src/udfStartNumberAssignDlg.h
	src/udfTarinersMngrDlg.h
	src/udfTeamCategoryMngrDlg.h
	src/udfTeamDancersMngrDlg.h
)

set( SOURCE_FILES 
	src/cdbmanager.cpp
	src/main.cpp
	src/udf.cpp
	src/udfAccountInfo.cpp
	src/udfCategoriesMngrDlg.cpp
	src/udfChampionshipCategoriesMngrDlg.cpp
	src/udfChampionshipJudgesTeamMngrDlg.cpp
	src/udfClubsMngrDlg.cpp
	src/udfDancersMngrDlg.cpp
	src/udfDancersTeamMngr.cpp
	src/udfDirectorInfo.cpp
	src/udfJudgesMngr.cpp
	src/udfMainFrameBase.cpp
	src/udfStartNumberAssignDlg.cpp
	src/udfTarinersMngrDlg.cpp
	src/udfTeamCategoryMngrDlg.cpp
	src/udfTeamDancersMngrDlg.cpp
)

# Make sure the PLATFORM_HEADER_FILES variable is always defined
set( PLATFORM_HEADER_FILES "" )

# Make sure the PLATFORM_SOURCE_FILES variable is always defined
set( PLATFORM_SOURCE_FILES "" )

# For ease of use make a variable that has all files for this module
set( ALL_FILES ${HEADER_FILES} ${SOURCE_FILES} ${PLATFORM_HEADER_FILES} ${PLATFORM_SOURCE_FILES} )

set( MODULE_NAME "gui" )


add_executable( ${MODULE_NAME} ${HEADER_FILES} ${SOURCE_FILES} )
add_dependencies( ${MODULE_NAME} dbmgr wxpng wxzlib wxexpat)
target_link_libraries( ${MODULE_NAME} dbmgr wxpng wxzlib wxexpat)
set_target_properties( ${MODULE_NAME} PROPERTIES COMPILE_DEFINITIONS "GUI_BUILD_MODULE" )

include_directories(
	${wxWidgetsPath}/include
	${UDF_SOURCE_DIR}/common/src 
	${UDF_SOURCE_DIR}/dbmgr/src 
	src
	res
)

link_directories ( ${UDF_BINARY_DIR}/lib ) 

set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall -L${UDF_BINARY_DIR}/lib") 

if ( WIN32 )
	include_directories( ${wxWidgetsBuild}/lib/gcc_lib/mswu )
	
	link_directories ( ${wxWidgetsBuild}/lib/gcc_lib ) 
	
	set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -L${wxWidgetsBuild}/lib/gcc_lib") 
	
	include_directories( ../gui/src/windows )
	
	add_dependencies( ${MODULE_NAME} wxmsw29u comctl32)
	target_link_libraries( ${MODULE_NAME} comctl32)
	
else ( WIN32 )
	MESSAGE(STATUS "Add path ${wxWidgetsPath}")
	MESSAGE(STATUS "Add build ${wxWidgetsBuild}")
	include_directories( ${wxWidgetsBuild}/lib/wx/include/gtk2-unicode-static-2.9 )
	include_directories( ${wxWidgetsBuild}/lib/wx/include/gtk2-unicode-2.9 )
	
	link_directories ( ${wxWidgetsBuild}/lib ) 
	
	set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -L${wxWidgetsBuild}/lib") 
	
	include_directories( ../gui/src/linux )
	
	add_dependencies( ${MODULE_NAME} wx_gtk2u-2.9 comctl32)
	target_link_libraries( ${MODULE_NAME} wx_gtk2u-2.9)
endif ( WIN32 )

